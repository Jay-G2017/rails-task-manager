<h2>details of this task</h2>

<h3><%= "Name:  #{@task.name}" %></h3>
<p><%= "Description: #{@task.description}" %></p>

<%= link_to "Delete", task_path(@task), method: :delete, data: { confirm: "Are you sure?" } %>

<%= link_to "Show all tasks", tasks_path %>
